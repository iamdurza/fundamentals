#!/usr/bin/env python3
if __name__ == '__main__':    
    import helloworld 
    message = "Hello " + who + "!";

    try:
        if option == "-m":
            multi(message)
        elif option == "-c":
            colored(message);
        else:
            plain(message);
    except KeyboardInterrupt:
        ()
